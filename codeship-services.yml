prebuild:
 image: yikaus/alpine-bash
 volumes:
    - .:/src

wat:
  build:
    image: dafire/wat
    dockerfile: Dockerfile
  volumes_from:
     - prebuild

nginx:
  build:
    image: dafire/wat-nginx
    dockerfile: Dockerfile.nginx
